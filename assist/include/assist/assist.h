using namespace excentury;

#define VECTOR DynamicTensor<1, double>
#define VECTOR_INT DynamicTensor<1, int>
#define VECTOR_OF(T) DynamicTensor<1, T >

#define MATRIX DynamicTensor<2, double>
#define MATRIX_INT DynamicTensor<2, int>
#define MATRIX_OF(T) DynamicTensor<2, T >

#define ASSIST_INCLUDE(M) <M>
